// Javascript(default) - Synchronous, Single threaded

// Execution Context - execute one line of code at a time
// each operation waits for the last one to complete before executing
// Call Stack 
// Memory Heap

// Blocking code vs Non-Blocking code
// Blocking - Block the flow of program (Read file sync)
// Non-Blocking - Does not block execution (Read file async)

// 1. In stack one by one program executes 
// 2. If any Web Api function called like setTimeout, setInterval then 1st other programs executes then that setTimeout function moves to queue then gos to stack & executes